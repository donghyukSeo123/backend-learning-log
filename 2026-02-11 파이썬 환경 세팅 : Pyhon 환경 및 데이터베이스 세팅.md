# 2026-02-11 – 백엔드 인프라 구축: Python 환경 및 데이터베이스 세팅

## 오늘 한 것
- **Python 가상환경(venv) 생성**: 프로젝트별 독립된 라이브러리 실행 환경을 구축하여 패키지 충돌 방지.
- **FastAPI 프레임워크 기반 설계**: 비동기 처리에 최적화된 백엔드 서버 기초 구조 수립.
- **PostgreSQL 데이터베이스 연동**: SQLAlchemy ORM을 활용한 파이썬 서버와 RDBMS 간의 커넥션 풀 설정.
- **의존성 관리 시스템 구축**: `pip freeze`를 이용한 설치 패키지 리스트(`requirements.txt`) 생성 및 관리.
- **DB 스키마 기초 정의**: 프로젝트 관리를 위한 테이블 구조(Project ID, Name 등) 설계.

## 오늘 알게 된 것
- **가상환경(Virtual Environment)**: 전역 파이썬 환경을 오염시키지 않고, 프로젝트마다 필요한 도구만 골라 담는 독립 공간의 중요성.
- **ORM(Object-Relational Mapping)**: 복잡한 SQL 쿼리 대신 파이썬 클래스와 객체로 데이터를 다루는 현대적인 DB 조작 방식.
- **ASGI 서버(Uvicorn)**: FastAPI를 실행하기 위한 비동기 서버 인터페이스의 역할과 실시간 반영(`--reload`) 옵션 활용법.
- **환경 변수 관리**: 서버 주소, 포트, DB 접속 정보 등 백엔드 실행에 필수적인 파라미터 설정 과정.

---

## 🧠 오늘의 개념

### 📦 파이썬 가상환경 (venv)
- **목적**: 프로젝트 독립성 유지 및 배포 환경의 일관성 확보.
- **주요 명령어**:
  - 생성: `python -m venv venv`
  - 활성화(Windows): `.\venv\Scripts\activate`

### 🗄️ 데이터베이스 연결 (SQLAlchemy Engine)
- **역할**: 파이썬 코드와 PostgreSQL 사이의 다리 역할.
- **연결 흐름**: `create_engine`을 통해 DB 주소를 등록하고, `sessionmaker`로 실제 데이터 입출력 단위를 생성함.

### 📜 의존성 명세 (Requirements)
- **정의**: 해당 프로젝트를 구동하기 위해 반드시 설치되어야 하는 라이브러리들의 명세서.
- **활용**: `pip install -r requirements.txt` 명령어를 통해 어디서든 동일한 개발 환경을 복구 가능하게 함.

### 📡 비동기 서버 (FastAPI & Uvicorn)
- **FastAPI**: 파이썬 3.6+의 표준 타입 힌트를 바탕으로 한 현대적이고 빠른 웹 프레임워크.
- **Uvicorn**: FastAPI의 실행 엔진 역할을 수행하며, 비동기 처리를 통해 높은 퍼포먼스를 유지함.

---

### 📌 요약
- **파이썬 가상환경**을 통해 깨끗한 개발 판을 깔고, **PostgreSQL**과 연결하여 데이터를 담을 견고한 그릇을 준비함.
- 백엔드 엔진의 기초가 되는 **환경 세팅**을 마무리함으로써, 실제 비즈니스 로직(API)을 구현하기 위한 모든 인프라 구축 완료.
